// Package internal contains packages that are only relevent to the sales-api command
// Placing them under internal/ provides an restriction from the compiler that
// they will not imported by any other commands or package outside of this tree

package internal
